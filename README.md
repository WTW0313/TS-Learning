# TS-Learning

Learning how to use TS

## How to run TS file?

```bash
npm i ts-node typescript -g

ts-node filename.ts

// like nodemon
npm i ts-node-dev -g

ts-node-dev --respawn --transpile-only filename.ts
```
